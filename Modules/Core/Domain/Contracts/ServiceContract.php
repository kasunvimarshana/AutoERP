<?php

declare(strict_types=1);

namespace Modules\Core\Domain\Contracts;

/**
 * Base service contract.
 *
 * Module services must implement this interface.
 * Services orchestrate use cases and define transaction boundaries.
 */
interface ServiceContract
{
    // Marker interface — concrete services define their own public methods.
}
