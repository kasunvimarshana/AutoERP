<?php

declare(strict_types=1);

namespace Modules\Core\Contracts;

/**
 * Base service contract
 *
 * Defines the standard interface for all service classes to ensure
 * consistent service layer implementation across modules.
 */
interface ServiceInterface
{
    // Marker interface to identify service layer components
    // Individual services define their own method contracts
}
