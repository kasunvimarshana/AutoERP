# Audit Module
Provides an immutable audit trail for all tenant operations.
## Features
- Record create/update/delete/view/export/login/logout events
- Filter by model type, model id, action, user
## Routes
- GET /api/v1/audit-logs
- GET /api/v1/audit-logs/{id}
