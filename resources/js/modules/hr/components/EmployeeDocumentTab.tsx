import { useMemo } from 'react';
import { documentApi } from '../hrApi';
import { SimpleEmployeeRelationTab } from './SimpleEmployeeRelationTab';
export function EmployeeDocumentTab({ employeeId }: { employeeId: number }) { const defaults = useMemo(() => ({ document_type: 'id_document', document_number: '', issued_date: '', expiry_date: '', file_path: '', status: 'pending', notes: '' }), []); return <SimpleEmployeeRelationTab employeeId={employeeId} title="Document" api={documentApi} defaults={defaults} tableFields={['document_type', 'document_number', 'expiry_date', 'status']} fields={[{ key: 'document_type', label: 'Type', type: 'select', options: ['id_document', 'contract', 'certification', 'license', 'medical', 'employment_letter', 'other'] }, { key: 'document_number', label: 'Number' }, { key: 'issued_date', label: 'Issued date', type: 'date' }, { key: 'expiry_date', label: 'Expiry date', type: 'date' }, { key: 'file_path', label: 'File path' }, { key: 'status', label: 'Status', type: 'select', options: ['active', 'expired', 'revoked', 'pending'] }, { key: 'notes', label: 'Notes' }]} />; }
