import { useMemo } from 'react';
import { contactApi } from '../hrApi';
import { SimpleEmployeeRelationTab } from './SimpleEmployeeRelationTab';
export function EmployeeContactTab({ employeeId }: { employeeId: number }) { const defaults = useMemo(() => ({ contact_name: '', relationship: '', email: '', phone: '', mobile: '', is_emergency_contact: false, is_primary: false, is_active: true }), []); return <SimpleEmployeeRelationTab employeeId={employeeId} title="Contact" api={contactApi} defaults={defaults} tableFields={['contact_name', 'relationship', 'phone', 'is_emergency_contact', 'is_primary']} fields={[{ key: 'contact_name', label: 'Name' }, { key: 'relationship', label: 'Relationship' }, { key: 'email', label: 'Email' }, { key: 'phone', label: 'Phone' }, { key: 'mobile', label: 'Mobile' }, { key: 'is_emergency_contact', label: 'Emergency contact', type: 'checkbox' }, { key: 'is_primary', label: 'Primary', type: 'checkbox' }, { key: 'is_active', label: 'Active', type: 'checkbox' }]} />; }
