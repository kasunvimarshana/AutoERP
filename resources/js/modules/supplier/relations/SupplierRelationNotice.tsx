export default function SupplierRelationNotice() {
    return <p className="text-xs text-slate-500">This relation panel is rendered only after its tab is opened. The current supplier detail API embeds all relations in one response.</p>;
}
