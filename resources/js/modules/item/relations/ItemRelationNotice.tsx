export default function ItemRelationNotice() {
    return <p className="text-xs text-slate-500">This relation panel is rendered on demand. The current Item detail endpoint still embeds all relation collections.</p>;
}
