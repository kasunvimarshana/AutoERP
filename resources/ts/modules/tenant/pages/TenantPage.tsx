import { ModulePlaceholderPage } from '../../../shared/components/business/ModulePlaceholderPage';

export function TenantPage() {
    return (
        <ModulePlaceholderPage
            description="Tenant profile, active organization context, subscriptions, feature flags, and isolation settings. Backend remains authoritative for tenant validation."
            title="Tenant"
        />
    );
}
