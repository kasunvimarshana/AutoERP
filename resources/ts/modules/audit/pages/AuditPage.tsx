import { ModulePlaceholderPage } from '../../../shared/components/business/ModulePlaceholderPage';

export function AuditPage() {
    return (
        <ModulePlaceholderPage
            description="Immutable audit logs, entity timelines, actor filters, and history views. Frontend only displays backend-captured audit records."
            title="Audit"
        />
    );
}
