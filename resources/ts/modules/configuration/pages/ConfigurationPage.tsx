import { ModulePlaceholderPage } from '../../../shared/components/business/ModulePlaceholderPage';

export function ConfigurationPage() {
    return (
        <ModulePlaceholderPage
            description="Configuration entries, resolved settings, feature switches, currencies, languages, countries, and module options. Backend resolves precedence and cache invalidation."
            title="Configuration"
        />
    );
}
