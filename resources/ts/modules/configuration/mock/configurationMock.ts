import type { ConfigurationRecord } from '../types/configuration.types';

export const configurationRecords: ConfigurationRecord[] = [
    { id: 'cfg-001', key: 'module.vehicle_service.enabled', scope: 'Tenant', status: 'Active' },
];
